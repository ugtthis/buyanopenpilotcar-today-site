[project]
name = "carmax-scraper"
version = "1.0.0"
description = "CarMax inventory scraper with openpilot car matching"
requires-python = ">=3.11"
dependencies = [
  "curl-cffi>=0.7.0",
]

[dependency-groups]
dev = [
  "pytest>=7.0.0",
  "pytest-asyncio>=1.3.0",
]
